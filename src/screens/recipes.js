import { getAvailability } from '../domain.js';
import { label, recipeCard } from '../ui.js';
export function renderRecipes({ recipes, state, filters = {} }) {
  const visible = recipes.filter(recipe => (!filters.search || `${recipe.name} ${recipe.localName}`.toLowerCase().includes(filters.search.toLowerCase())) && (!filters.tag || recipe.dietaryTags.includes(filters.tag)) && (!filters.stocked || getAvailability(recipe, state.pantry).availableCount > 0));
  const tags = ['halal', 'vegetarian', 'no-pork', 'no-seafood'];
  return `<section class="screen"><div class="screen-heading"><div><p class="eyebrow">Curated for everyday kitchens</p><h1>Recipes</h1></div><span class="recipe-count">${visible.length}<small>ideas</small></span></div><label class="search-box"><span>&#8981;</span><input data-filter="search" value="${filters.search ?? ''}" placeholder="Search nasi, mee, telur..." aria-label="Search recipes" /></label><div class="filter-row"><button class="filter-chip ${!filters.tag ? 'active' : ''}" data-filter-tag="">All</button>${tags.map(tag => `<button class="filter-chip ${filters.tag === tag ? 'active' : ''}" data-filter-tag="${tag}">${label(tag)}</button>`).join('')}<button class="filter-chip ${filters.stocked ? 'active' : ''}" data-filter-stocked="true">Use what I have</button></div>${visible.length ? `<div class="recipe-grid">${visible.map(recipe => recipeCard(recipe, getAvailability(recipe, state.pantry))).join('')}</div>` : '<div class="empty-state compact"><span class="empty-emoji">&#127834;</span><h2>Nothing matches yet</h2><p>Try a broader search or let one pantry ingredient lead the way.</p><button class="primary-button" data-filter-tag="">Browse all</button></div>'}</section>`;
}